<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a10in1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Second_Activity"></activity>
        <activity android:name=".Activity_switcher" />
        <activity android:name=".Web_pages" />
        <activity android:name=".Image_magnifier" />
        <activity android:name=".Radio_color" />
        <activity android:name=".Playing_Sound" />
        <activity android:name=".layouts" />
        <activity android:name=".Clickable" />
        <activity android:name=".button_toast" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>